# Copy this file to .env and add your real OpenWeather API key.
# The .env file is gitignored — it will NEVER be committed or exposed to the browser.

OPENWEATHER_API_KEY=your_api_key_here
# Optional: change the port (default 3000)
# PORT=3000
